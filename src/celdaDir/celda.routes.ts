/*

import { Router } from "express";
import { getAll, getOne, update, add, deleteOne, sanitizarInputDeActividad } from "./celda.controller.js";

export const celdaRouter = Router()

celdaRouter.get('/', getAll)
celdaRouter.get('/:cod_actividad', getOne)
celdaRouter.post('/', sanitizarInputDeCelda, add)
celdaRouter.put('/:cod_celda', sanitizarInputDeCelda, update)
celdaRouter.delete('/:cod_celda', deleteOne)


*/